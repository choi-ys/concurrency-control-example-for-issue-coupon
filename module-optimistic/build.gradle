dependencies {
    api project(':module-common')
    testImplementation project(":module-common").sourceSets.test.output
    testImplementation 'io.github.hakky54:consolecaptor:1.0.2'
}

bootJar { enabled(false) }
jar { enabled(true) }
